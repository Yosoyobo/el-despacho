from django.apps import AppConfig


class AuthTallerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.auth_taller"
    label = "auth_taller"
    verbose_name = "Auth — El Taller"
