from django.apps import AppConfig


class DirectorioConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.directorio"
    label = "directorio_taller"
    verbose_name = "Directorio (Taller)"
