# El Despacho — variables de entorno
# Solo se permiten aquí: secretos de arranque y conexión a Postgres/Redis.
# TODAS las demás credenciales (Google, Stripe, MercadoPago, IA, n8n) se
# configuran desde el UI de Los Ajustes en La Gerencia, cifradas con La Bóveda.

# 64 hex chars (32 bytes). Genera con: python -c "import secrets;print(secrets.token_hex(32))"
BOVEDA_MASTER_KEY=

# 64 hex chars. Misma para ambas apps (sirve como Django SECRET_KEY).
DJANGO_SECRET_KEY=

# Postgres
POSTGRES_DB=el_despacho
POSTGRES_USER=despacho
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0

# Hosts permitidos por Django (coma-separados)
GERENCIA_ALLOWED_HOSTS=gerencia.learningcenter.mx,localhost,127.0.0.1
TALLER_ALLOWED_HOSTS=taller.learningcenter.mx,localhost,127.0.0.1
RECEPCION_ALLOWED_HOSTS=recepcion.learningcenter.mx,localhost,127.0.0.1

# Bootstrap del primer super_admin (idempotente en cada arranque)
DESPACHO_SUPERADMIN_EMAIL=oscar@bautista.mx
DESPACHO_SUPERADMIN_PASSWORD=cambiar-al-primer-login

# Caddy local (HAL): macOS reserva 80/443, usamos 18080/18443.
CADDY_HTTP_PORT=18080
CADDY_HTTPS_PORT=18443

# Entorno (development | production)
DESPACHO_ENV=development

# ── El Celador: credencial del monitor del taller (opcional) ───────────────────
# El monitor pega a GET /salud y lo manda en la cabecera `x-celador`. Con él,
# /salud contesta también el desglose (gasto de IA y uso); sin él, solo la cara
# pública. El camino normal es pegarlo en Los Ajustes (slot `celador_token`,
# cifrado con La Bóveda); esta variable es el respaldo del contrato del taller y
# sirve para arrancar antes de tener acceso al GUI.
# VACÍO = NADIE pasa (se cierra, no se abre). El token lo entrega el taller y es
# el mismo para todas las máquinas y desarrollos: trátalo como una credencial.
CELADOR_TOKEN=

# ── El Resguardo: backup offsite a DigitalOcean Spaces (opcional) ──────────────
# Excepción a la regla de "todo en Los Ajustes": archivo.sh es bash en el host
# y NO puede descifrar La Bóveda (que vive dentro del contenedor Django). Por
# eso estas llaves van en el .env del Droplet, igual que HAL_KEY/POSTGRES_USER.
# DORMIDO si vacías: archivo.sh detecta la ausencia y salta el push sin fallar.
# Pasos para activarlo: docs/SETUP_RESGUARDO.md
DO_SPACES_KEY=
DO_SPACES_SECRET=
DO_SPACES_BUCKET=
DO_SPACES_REGION=nyc3
DO_SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
# RESGUARDO_RETENER=30   # informativo; la rotación real la hace el lifecycle del Space
# SKIP_RESGUARDO=1       # saltar el push puntualmente
