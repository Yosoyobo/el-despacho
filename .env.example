# El Despacho — variables de entorno
# Solo se permiten aquí: secretos de arranque y conexión a Postgres/Redis.
# TODAS las demás credenciales (Google, Stripe, MercadoPago, IA, n8n) se
# configuran desde el UI de Los Ajustes en La Gerencia, cifradas con La Bóveda.

# 64 hex chars (32 bytes). Genera con: python -c "import secrets;print(secrets.token_hex(32))"
BOVEDA_MASTER_KEY=

# 64 hex chars. Misma para ambas apps (sirve como Django SECRET_KEY).
DJANGO_SECRET_KEY=

# Postgres
POSTGRES_DB=el_despacho
POSTGRES_USER=despacho
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0

# Hosts permitidos por Django (coma-separados)
GERENCIA_ALLOWED_HOSTS=gerencia.learningcenter.mx,localhost,127.0.0.1
TALLER_ALLOWED_HOSTS=taller.learningcenter.mx,localhost,127.0.0.1
RECEPCION_ALLOWED_HOSTS=recepcion.learningcenter.mx,localhost,127.0.0.1

# Bootstrap del primer super_admin (idempotente en cada arranque)
DESPACHO_SUPERADMIN_EMAIL=oscar@bautista.mx
DESPACHO_SUPERADMIN_PASSWORD=cambiar-al-primer-login

# Caddy local (HAL): macOS reserva 80/443, usamos 18080/18443.
CADDY_HTTP_PORT=18080
CADDY_HTTPS_PORT=18443

# Entorno (development | production)
DESPACHO_ENV=development
