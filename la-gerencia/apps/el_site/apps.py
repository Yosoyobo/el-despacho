from django.apps import AppConfig


class ElSiteConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.el_site"
    label = "el_site"
    verbose_name = "El Site"
