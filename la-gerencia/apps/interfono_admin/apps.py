from django.apps import AppConfig


class InterfonoAdminConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.interfono_admin"
    label = "interfono_admin"
    verbose_name = "El Interfono — Admin"
