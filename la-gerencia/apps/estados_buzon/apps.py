from django.apps import AppConfig


class EstadosBuzonConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.estados_buzon"
    label = "estados_buzon_admin"
    verbose_name = "Estados del Buzón (admin)"
