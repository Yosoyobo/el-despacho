from django.apps import AppConfig


class CentrosCostoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.centros_costo"
    label = "centros_costo_admin"
    verbose_name = "Centros de costo (admin)"
