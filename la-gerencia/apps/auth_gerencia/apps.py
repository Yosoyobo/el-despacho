from django.apps import AppConfig


class AuthGerenciaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.auth_gerencia"
    label = "auth_gerencia"
    verbose_name = "Auth — La Gerencia"
