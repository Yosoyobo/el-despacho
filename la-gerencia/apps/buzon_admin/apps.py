from django.apps import AppConfig


class BuzonAdminConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.buzon_admin"
    label = "buzon_admin"
    verbose_name = "El Buzón — admin"
