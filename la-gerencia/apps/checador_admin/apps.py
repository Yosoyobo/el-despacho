from django.apps import AppConfig


class CheckadorAdminConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.checador_admin"
    label = "checador_admin"
    verbose_name = "El Checador (admin)"
