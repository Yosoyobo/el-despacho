from django.apps import AppConfig


class EstadosProyectoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.estados_proyecto"
    label = "estados_proyecto_admin"
    verbose_name = "Estados de proyecto (admin)"
