from django.apps import AppConfig


class EstadosTareaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.estados_tarea"
    label = "estados_tarea_admin"
    verbose_name = "Estados de tarea (admin)"
