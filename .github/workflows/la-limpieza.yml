name: La Limpieza

on:
  schedule:
    - cron: "0 6 * * 0"  # domingos 06:00 UTC — solo dispara poda-ghcr
  workflow_dispatch:

jobs:
  poda-ghcr:
    name: Podar tags antiguos de GHCR
    runs-on: ubuntu-latest
    permissions:
      packages: write
    strategy:
      matrix:
        package: [el-despacho-gerencia, el-despacho-taller, el-despacho-recepcion]
    steps:
      - name: Mantener solo las últimas 10 versiones
        uses: actions/delete-package-versions@v5
        with:
          package-name: ${{ matrix.package }}
          package-type: container
          min-versions-to-keep: 10
          delete-only-untagged-versions: false

  limpiar-disco:
    name: 🧹 Liberar disco en el NUC de Learning Center
    # Solo manual — el cron semanal sigue podando GHCR, pero el disco se limpia a voluntad.
    #
    # Desde la mudanza del 2026-08-21 el stack vive en el NUC, que solo existe en el
    # tailnet. El `if` exige el cliente OAuth: sin él, el job se salta.
    if: github.event_name == 'workflow_dispatch' && secrets.TS_OAUTH_CLIENT_ID != ''
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 🔌 Entrar al tailnet (el NUC solo existe ahí)
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
      - name: 🛡️ Pre-flight — verificar que el stack está running
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.NUC_HOST }}
          username: ${{ secrets.NUC_USER }}
          key: ${{ secrets.NUC_SSH_KEY }}
          command_timeout: 2m
          script: |
            set -euo pipefail
            cd /mnt/el-despacho
            NO_UP=$(docker compose -f docker-compose.yml -f docker-compose.prod.yml ps --format json \
              | jq -r 'select(.State != "running") | .Service' || true)
            if [ -n "$NO_UP" ]; then
              echo "❌ ABORT: estos servicios NO están running:"
              echo "$NO_UP"
              echo "La Limpieza no procede hasta que el stack esté completamente healthy."
              exit 1
            fi
            echo "✅ Pre-flight OK: los 7 servicios están running."

      - name: 🧹 Limpiar Docker + logs + apt + tmp + backups
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.NUC_HOST }}
          username: ${{ secrets.NUC_USER }}
          key: ${{ secrets.NUC_SSH_KEY }}
          command_timeout: 8m
          script: |
            # NO -e: cada paso es tolerante; un fallo aislado no aborta los demás.
            set -uo pipefail

            echo "=== DISCO ANTES ==="
            df -h /
            echo ""
            echo "=== DOCKER ANTES ==="
            docker system df
            echo ""

            echo "=== 1) docker system prune (SIN --volumes) ==="
            docker system prune -af
            echo ""

            echo "=== 2) Volúmenes Docker huérfanos (solo listado, NO se borran) ==="
            DANGLING=$(docker volume ls -qf dangling=true || true)
            if [ -z "$DANGLING" ]; then
              echo "Sin volúmenes huérfanos."
            else
              echo "Volúmenes huérfanos detectados:"
              echo "$DANGLING"
              echo "NO se borran automáticamente (decisión de seguridad)."
              echo "Revísalos manualmente vía SSH si confirmas que son basura."
            fi
            echo ""

            echo "=== 3) journald > 7 días ==="
            sudo journalctl --vacuum-time=7d 2>/dev/null || true
            echo ""

            echo "=== 4) /tmp > 1 día ==="
            sudo find /tmp -maxdepth 1 -mtime +1 -delete 2>/dev/null || true
            echo ""

            echo "=== 5) apt autoremove + clean ==="
            sudo apt-get autoremove -y --purge 2>/dev/null || true
            sudo apt-get clean 2>/dev/null || true
            echo ""

            echo "=== 6) Rotar backups locales (conservar últimos 5 de cada serie) ==="
            if [ -d /mnt/el-despacho/backups ]; then
              cd /mnt/el-despacho/backups
              ls -t db-*.sql.gz 2>/dev/null | tail -n +6 | xargs -r rm -v
              ls -t credenciales-*.tar.gz 2>/dev/null | tail -n +6 | xargs -r rm -v
            else
              echo "Sin directorio /mnt/el-despacho/backups todavía."
            fi
            echo ""

            echo "=== DOCKER DESPUÉS ==="
            docker system df
            echo ""
            echo "=== DISCO DESPUÉS ==="
            df -h /
            echo ""
            echo "✅ Limpieza completada"

      - name: 🩺 Post-flight — verificar que el stack sigue healthy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.NUC_HOST }}
          username: ${{ secrets.NUC_USER }}
          key: ${{ secrets.NUC_SSH_KEY }}
          command_timeout: 2m
          script: |
            set -euo pipefail
            cd /mnt/el-despacho
            docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
            NO_UP=$(docker compose -f docker-compose.yml -f docker-compose.prod.yml ps --format json \
              | jq -r 'select(.State != "running") | .Service' || true)
            if [ -n "$NO_UP" ]; then
              echo "❌ POST-FLIGHT FAILED: estos servicios cayeron tras la limpieza:"
              echo "$NO_UP"
              echo "Revisa logs con: docker compose -f docker-compose.yml -f docker-compose.prod.yml logs <servicio> --tail 100"
              exit 1
            fi
            echo "✅ Stack sigue healthy."

      - name: 🚥 Smoke test de las 3 URLs HTTPS
        run: |
          set -e
          for host in gerencia taller recepcion; do
            CODE=$(curl -fsS -o /dev/null -w "%{http_code}" "https://${host}.learningcenter.mx/ping" || echo "FAIL")
            if [ "$CODE" != "200" ]; then
              echo "❌ https://${host}.learningcenter.mx/ping respondió $CODE"
              exit 1
            fi
            echo "✅ https://${host}.learningcenter.mx/ping → 200"
          done
